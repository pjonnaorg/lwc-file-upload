#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@salesforce/sfdx-lwc-jest/bin/sfdx-lwc-jest" "$@"
  ret=$?
else 
  node  "$basedir/../@salesforce/sfdx-lwc-jest/bin/sfdx-lwc-jest" "$@"
  ret=$?
fi
exit $ret
